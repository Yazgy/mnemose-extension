# Base URL of the Mnemose instance. This value is used by production builds
# (`wxt build` / `wxt zip` / the signed .xpi). Local dev overrides it via
# .env.development.
WXT_MNEMOSE_URL=https://mnemose.com
